# ============================================================================
# requirements.txt — dépendances de l'APPLICATION STREAMLIT DÉPLOYÉE uniquement
# (le notebook Jupyter local utilise en plus prophet/xgboost/statsmodels/
#  matplotlib/seaborn, déjà installés dans votre venv local — inutile de les
#  redéployer sur Streamlit Cloud, ça n'allège que le déploiement en ligne)
# ============================================================================

# --- Application web ---
streamlit>=1.38

# --- Manipulation de données ---
pandas>=2.0
numpy>=1.26

# --- Visualisation (Dashboard, Prévisions) ---
plotly>=5.20

# --- Modèle de prévision utilisé en production ---
scikit-learn>=1.4
lightgbm>=4.5
joblib>=1.4

# --- RAG : base vectorielle + embeddings ---
chromadb>=0.5
# ChromaDB utilise son modèle d'embedding ONNX intégré (léger, sans PyTorch)
# via embedding_functions.DefaultEmbeddingFunction()

# --- LLM (Groq - gratuit, pas de carte bancaire) ---
groq>=0.11

# --- Export des rapports ---
python-docx>=1.1
fpdf2>=2.7

# --- Utilitaires ---
python-dotenv>=1.0