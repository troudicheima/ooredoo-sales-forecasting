# ============================================================================
# requirements.txt — dépendances de l'APPLICATION STREAMLIT DÉPLOYÉE uniquement
# (le notebook Jupyter local utilise en plus prophet/xgboost/statsmodels/
#  matplotlib/seaborn, déjà installés dans votre venv local — inutile de les
#  redéployer sur Streamlit Cloud, ça n'allège que le déploiement en ligne)
# ============================================================================

# --- Application web ---
streamlit>=1.38

# --- Manipulation de données ---
pandas>=2.0
numpy>=1.26

# --- Visualisation (Dashboard, Prévisions) ---
plotly>=5.20

# --- Modèle de prévision utilisé en production ---
scikit-learn>=1.4
lightgbm>=4.5
joblib>=1.4

# --- RAG : base vectorielle + embeddings ---
# IMPORTANT : version FIGÉE (==) et non "au moins" (>=) -> le format de
# sauvegarde interne de ChromaDB change parfois entre versions ; si votre
# PC et Streamlit Cloud installent des versions différentes, le vector_store
# construit en local devient illisible une fois déployé (erreur
# "Could not build embedding function ... from config"). En figeant la
# version ici ET en installant exactement celle-ci en local avant de
# reconstruire le vector_store, on garantit que les deux environnements
# utilisent le même format.
chromadb==1.5.9
# ChromaDB utilise son modèle d'embedding ONNX intégré (léger, sans PyTorch)
# via embedding_functions.DefaultEmbeddingFunction()

# --- LLM (Groq - gratuit, pas de carte bancaire) ---
groq>=0.11

# --- Export des rapports ---
python-docx>=1.1
fpdf2>=2.7

# --- Utilitaires ---
python-dotenv>=1.0